# update/academyinfo/include

update 보조 공통 파일 위치.

현재 서버에서 확인된 파일:
- `common.py`
- `common_local.py` (서버 전용 비밀값)

주의:
- 저장소에는 `common.py`와 `common_local.py.example`만 둔다
- 실제 비밀값은 `common_local.py`로 서버에만 둔다
